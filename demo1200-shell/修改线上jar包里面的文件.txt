查找文件在jar里面的路径
jar -tvf demo.jar|grep application.yml
4486 Tue May 21 10:22:56 CST 2024 BOOT-INF/classes/application.yml


解压需要修改的文件
jar -xvf demo.jar BOOT-INF/classes/application.yml
已解压: BOOT-INF/classes/application.yml

# 替换application.yml
cp application.yml BOOT-INF/classes/application.yml

# 修改application.yml
vi BOOT-INF/classes/application.yml
jar -uvf demo.jar BOOT-INF/classes/application.yml
正在添加: BOOT-INF/classes/application.yml(输入 = 4442) (输出 = 1693)(压缩了 61%)


替换jar里面的jar文件
# 解压文件
jar -xvf demo.jar
# 替换其中的文件
mv test1.0.0.jar BOOT-INF/lib/test1.0.0.jar
# 重新打包
jar -cfM0 demo2.jar BOOT-INF/ META-INF/ org/


jar各项参数解释
参数其中cxut这四个参数必须选其一。vfmn0PMe是可选参数，另外文件名也是必须的。

参数	解释
-c	创建一个jar包
-x	解压jar包
-u	添加文件到jar包中
-t	显示jar中的内容列表
-i	为指定的jar文件创建索引文件
-f	指定jar包的文件名
-v	生成详细的报造，并输出至标准设备
-m	指定manifest.mf文件.(manifest.mf文件中可以对jar包及其中的内容作一些一设置)
-0	产生jar包时不对其中的内容进行压缩处理
-M	不产生所有文件的清单文件(Manifest.mf)。这个参数与忽略掉-m参数的设置
-P	保留文件名中的前导’/'（绝对路径）和“…”（父目录）组件
-e	为独立应用程序指定应用程序入口点捆绑到可执行jar文件中
-C	表示转到相应的目录下执行jar命令,相当于cd到那个目录，然后不带-C执行jar命令